import 'package:flutter/material.dart';

class BottomCornner extends StatelessWidget {
  const BottomCornner({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'images/attendance 1.png',
      fit: BoxFit.cover,
    );
  }
}
